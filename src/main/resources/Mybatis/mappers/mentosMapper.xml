<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shinhan.memento.mapper.MentosMapper">

	<resultMap type="com.shinhan.memento.model.Mentos"
		id="mentosMapper">
		<id property="mentosId" column="mentos_id" />
		<result property="categoryId" column="category_id" />
		<result property="languageId" column="language_id" />
		<result property="mentoId" column="mento_id" />
		<result property="title" column="title" />
		<result property="simpleContent" column="simple_content" />
		<result property="image" column="image" />
		<result property="price" column="price" />
		<result property="regionGroup" column="region_group" />
		<result property="regionSubgroup" column="region_subgroup" />
		<result property="regionDetail" column="region_detail" />
		<result property="startDay" column="start_day" />
		<result property="endDay" column="end_day" />
		<result property="startTime" column="start_time" />
		<result property="endTime" column="end_time" />
		<result property="selectedDays" column="selected_days" />
		<result property="times" column="times" />
		<result property="minMember" column="min_member" />
		<result property="maxMember" column="max_member" />
		<result property="content" column="content" />
		<result property="createdAt" column="created_at" />
		<result property="updatedAt" column="updated_at" />
		<result property="status" column="status" />
		<result property="matchTypeIdFirst"
			column="match_type_id_first" />
		<result property="matchTypeIdSecond"
			column="match_type_id_second" />
		<result property="matchTypeIdThird"
			column="match_type_id_third" />
	</resultMap>

	<insert id="createMentos"
		parameterType="com.shinhan.memento.dto.CreateMentosDBDTO">
		INSERT INTO mentos (
		mentos_id, category_id, language_id,
		mento_id, title, simple_content,
		image, min_member, max_member,
		start_day, end_day,
		start_time, end_time, selected_days, region_group,
		region_subgroup,
		region_detail,
		content, price, times, created_at,
		updated_at, status,
		match_type_first, match_type_second,
		match_type_third
		) VALUES (
		seq_mentos_id.nextval, #{categoryId},
		#{languageId}, #{mentoId}, #{title}, #{simpleContent},
		#{image},
		#{minMember}, #{maxMember}, #{startDay}, #{endDay},
		#{startTime},
		#{endTime}, #{selectedDays}, #{regionGroup},
		#{regionSubgroup},
		#{regionDetail},
		#{content}, #{price}, #{times}, sysdate, null,
		'ACTIVE',
		#{matchTypeFirst}, #{matchTypeSecond}, #{matchTypeThird}
		)
	</insert>


	<resultMap id="findMentosByMentosId"
		type="com.shinhan.memento.dto.DetailMentosDTO" autoMapping="false">
		<constructor>
			<idArg column="mentos_id" javaType="int" />
			<arg column="title" javaType="String" />
			<arg column="mento_name" javaType="String" />
			<arg column="start_day" javaType="java.sql.Timestamp" />
			<arg column="end_day" javaType="java.sql.Timestamp" />
			<arg column="start_time" javaType="java.sql.Timestamp" />
			<arg column="end_time" javaType="java.sql.Timestamp" />
			<arg column="selectedDay" javaType="String" />
			<arg column="regionSubGroup" javaType="String" />
			<arg column="price" javaType="int" />
			<arg column="image" javaType="String" />
			<arg column="categoryName" javaType="String" />
			<arg column="languageName" javaType="String" />
			<arg column="simpleContent" javaType="String" />
		</constructor>
		<result property="matchTypeIdFirst"
			column="match_type_id_first" />
		<result property="matchTypeIdSecond"
			column="match_type_id_second" />
		<result property="matchTypeIdThird"
			column="match_type_id_third" />
	</resultMap>


	<select id="findMentosByMentosId" parameterType="map"
		resultMap="findMentosByMentosId">
		SELECT
		m.mentos_id,
		m.title,
		m.start_day,
		m.end_day,
		m.start_time,
		m.end_time,
		m.selected_days AS selectedDay,
		m.region_subgroup AS
		regionSubGroup,
		m.price,
		m.image,
		m.simple_content AS simpleContent,
		m.match_type_id_first,
		m.match_type_id_second,
		m.match_type_id_third,
		c.category_name AS categoryName,
		l.language_name AS languageName,
		mto.nickname AS mento_name
		FROM mentos m
		JOIN category c ON m.category_id = c.category_id
		JOIN language l ON m.language_id = l.language_id
		JOIN member mto ON m.mento_id = mto.member_id
		WHERE m.mentos_id = #{mentosId}
		<if test="status != null">
			AND m.status = #{status}
		</if>
	</select>


	<resultMap id="similarMentosMap"
		type="com.shinhan.memento.dto.DetailMentosDTO$SimilarMentosListDTO">
		<id property="mentosId" column="mentos_id" />
		<result property="title" column="title" />
		<result property="mentoName" column="mento_name" />
		<result property="startDayRaw" column="start_day" />
		<result property="endDayRaw" column="end_day" />
		<result property="startTimeRaw" column="start_time" />
		<result property="endTimeRaw" column="end_time" />
		<result property="selectedDay" column="selectedDay" />
		<result property="regionSubGroup" column="regionSubGroup" />
		<result property="price" column="price" />
		<result property="image" column="image" />
		<result property="simpleContent" column="simpleContent" />
		<result property="categoryName" column="categoryName" />
		<result property="languageName" column="languageName" />
	</resultMap>


	<select id="findSimilarMentos" resultMap="similarMentosMap"
		parameterType="map">
  <![CDATA[
    SELECT * FROM (
      SELECT
        m.mentos_id,
        m.title,
        m.start_day,
        m.end_day,
        m.start_time,
        m.end_time,
        m.selected_days AS selectedDay,
        m.region_subgroup AS regionSubGroup,
        m.price,
        m.image,
        m.simple_content AS simpleContent,
        c.category_name AS categoryName,
        l.language_name AS languageName,
        mto.nickname AS mento_name
      FROM mentos m
      JOIN category c ON m.category_id = c.category_id
      JOIN language l ON m.language_id = l.language_id
      JOIN member mto ON m.mento_id = mto.member_id
      WHERE
        m.region_group = #{regionGroup}
        AND m.region_subgroup = #{regionSubgroup}
        AND m.language_id = #{languageId}
        AND m.mentos_id != #{mentosId}
        AND m.status = 'ACTIVE'
      ORDER BY m.mentos_id DESC
    )
    WHERE ROWNUM <= 3
  ]]>
	</select>


</mapper>


